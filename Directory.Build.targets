<Project>
  <PropertyGroup Condition="$([MSBuild]::VersionGreaterThanOrEquals($(NETCoreSdkVersion), '7.0'))">
    <DefineConstants>$(DefineConstants);NET7SDK</DefineConstants>
  </PropertyGroup>
  <Import Project="build/ForkOverride.targets" Condition="'$(ForkVersion)' != ''" />
</Project>
