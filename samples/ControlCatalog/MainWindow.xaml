<Window xmlns="https://github.com/avaloniaui" MinWidth="500" MinHeight="300"
        Width="1024" Height="800"
        xmlns:pages="clr-namespace:ControlCatalog.Pages"
        Title="Avalonia Control Gallery"
        Icon="/Assets/test_icon.ico"
        xmlns:local="clr-namespace:ControlCatalog"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ControlCatalog.ViewModels"
        xmlns:v="clr-namespace:ControlCatalog.Views"
        x:Class="ControlCatalog.MainWindow" WindowState="{Binding WindowState, Mode=TwoWay}" Background="Transparent">
  <DockPanel>
    <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" VerticalAlignment="Top" Spacing="5">
      <Border Background="LightGray" BorderThickness="1" CornerRadius="4">
        <Button Content="Test" />
      </Border>

      <Border Background="LightGray" BorderThickness="1" CornerRadius="4">
        <Button Content="Test" />
      </Border>

      <Border Background="LightGray" BorderThickness="1" CornerRadius="4">
        <Button Content="Test" />
      </Border>

      <Border Background="LightGray" BorderThickness="1" CornerRadius="4">
        <Button Content="Test" />
      </Border>

      <Border Background="LightGray" BorderThickness="1" CornerRadius="4">
        <Button Content="Test" />
      </Border>
    </StackPanel>
  </DockPanel>
  
</Window>
