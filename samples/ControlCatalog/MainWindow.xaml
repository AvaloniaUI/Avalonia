<Window xmlns="https://github.com/avaloniaui" MinWidth="500" MinHeight="300"
        Width="1024" Height="800"
        xmlns:pages="clr-namespace:ControlCatalog.Pages"
        Title="Avalonia Control Gallery"
        Icon="/Assets/test_icon.ico"
        xmlns:local="clr-namespace:ControlCatalog"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ControlCatalog.ViewModels"
        xmlns:v="clr-namespace:ControlCatalog.Views"
        x:Class="ControlCatalog.MainWindow" WindowState="{Binding WindowState, Mode=TwoWay}" Background="Transparent" TextBlock.Foreground="Red">
  <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Border.Background>
      <ImageBrush Source="/Assets/clovers.jpg"/>
    </Border.Background>
    <Panel>
      <StackPanel Margin="24" Spacing="24">
        <Border Height="100">
          <Border.Background>
            <AcrylicBrush TintOpacity=".6" TintColor="White" BackgroundSource="BackDrop"  />
          </Border.Background>
        </Border>

        <Border Height="100">
          <Border.Background>
            <AcrylicBrush TintOpacity=".6" TintColor="White" BackgroundSource="HostBackDrop"  />
          </Border.Background>
        </Border>

        <Border Height="100">
          <Border.Background>
            <AcrylicBrush TintOpacity=".4" TintColor="Purple" BackgroundSource="HostBackDrop"  />
          </Border.Background>
        </Border>

        <Border Height="100">
          <Border.Background>
            <AcrylicBrush TintOpacity=".15" TintColor="Red" TintLuminosityOpacity="0.15" BackgroundSource="HostBackDrop"  />
          </Border.Background>
        </Border>
      
      <Border Height="100">
          <Border.Background>
            <AcrylicBrush TintOpacity="0" TintColor="Purple" TintLuminosityOpacity="0" BackgroundSource="HostBackDrop"  />
          </Border.Background>
        </Border>
      </StackPanel>



      <StackPanel Margin="50 50 0 0" Width="250">
        <Slider x:Name="ColourBurn" Minimum="0" Maximum="0.3" Value="0.1" />
        <Slider x:Name="TintOpacity" Minimum="0.6" Maximum="0.8" Value="0.8" />
        <Slider x:Name="NoiseLevel" Minimum="0" Maximum="1" Value="0.12" />
      </StackPanel>
    </Panel>
  </Border>
</Window>
