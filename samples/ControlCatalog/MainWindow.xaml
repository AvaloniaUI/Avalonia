<Window xmlns="https://github.com/avaloniaui" MinWidth="500" MinHeight="300"
        Width="1024" Height="800"
        xmlns:pages="clr-namespace:ControlCatalog.Pages"
        Title="Avalonia Control Gallery"
        Icon="/Assets/test_icon.ico"
        xmlns:local="clr-namespace:ControlCatalog"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ControlCatalog.ViewModels"
        xmlns:v="clr-namespace:ControlCatalog.Views"
        x:Class="ControlCatalog.MainWindow" WindowState="{Binding WindowState, Mode=TwoWay}" Background="Transparent" TextBlock.Foreground="Red">

  <Panel>
    <StackPanel>
      <TextBlock VerticalAlignment="Top" HorizontalAlignment="Left" Text="Desktop Background / Gausian Blur (1/2)" />      
      <Slider x:Name="ColourBurn" Minimum="0" Maximum="0.3" Value="0.1" />
      <Slider x:Name="TintOpacity" Minimum="0.6" Maximum="0.8" Value="0.8" />
      <Slider x:Name="NoiseLevel" Minimum="0" Maximum="1" Value="0.12" />
    </StackPanel>
    <Border Margin="100" BorderThickness="1" BorderBrush="Black">
      <Border.Background>
        <SolidColorBrush Color="White" Opacity="{Binding #ColourBurn.Value}" />
      </Border.Background>
      <Panel>
        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Left" Text="Colour Burn (3)" />
        <Border Margin="100" BorderThickness="1" BorderBrush="Black">
          <Border.Background>
            <SolidColorBrush Color="White" Opacity="{Binding #TintOpacity.Value}" />
          </Border.Background>
          <Panel>
            <TextBlock VerticalAlignment="Top" HorizontalAlignment="Left" Text="Tint (4)" />
            <Border Margin="100" BorderThickness="1" BorderBrush="Black">
              <Border.Background>
                <ImageBrush Source="/Assets/noise2.png" Opacity="{Binding #NoiseLevel.Value}"  DestinationRect="0 0 40 40" TileMode="Tile" />
              </Border.Background>
              <Panel>
                <TextBlock VerticalAlignment="Top" HorizontalAlignment="Left" Text="Noise (5)" />
                <TextBlock VerticalAlignment="Top" HorizontalAlignment="Right" Text="{Binding #NoiseLevel.Value}" />
              </Panel>
            </Border>
          </Panel>
        </Border>
      </Panel>
    </Border>
  </Panel>
</Window>
