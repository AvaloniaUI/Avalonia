<Window xmlns="https://github.com/avaloniaui" MinWidth="500" MinHeight="300"
        Width="1024" Height="800"
        xmlns:pages="clr-namespace:ControlCatalog.Pages"
        Title="Avalonia Control Gallery"
        Icon="/Assets/test_icon.ico"
        xmlns:local="clr-namespace:ControlCatalog"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ControlCatalog.ViewModels"
        xmlns:v="clr-namespace:ControlCatalog.Views"
        x:Class="ControlCatalog.MainWindow" WindowState="{Binding WindowState, Mode=TwoWay}">

  <ScrollViewer Name="scroller" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
    <ItemsRepeater Name="repeater" Background="Transparent" Items="{Binding Albums}" Margin="0 60">      
      <ItemsRepeater.Layout>
        <UniformGridLayout ItemsJustification="SpaceEvenly" MinRowSpacing="60" Orientation="Horizontal" MaximumRowsOrColumns="4" />
      </ItemsRepeater.Layout>
      <ItemsRepeater.ItemTemplate>
        <DataTemplate>

          <Border BorderBrush="Black" Height="200" Width="200" BorderThickness="1">
            <Panel>


              <Panel>
                <TextBlock Text="{Binding }" FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center" />
              </Panel>
            </Panel>
          </Border>


        </DataTemplate>
      </ItemsRepeater.ItemTemplate>
    </ItemsRepeater>
  </ScrollViewer>
</Window>
