<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="ControlCatalog.Pages.TabbedPagePerformancePage">
  <DockPanel>
    <ScrollViewer DockPanel.Dock="Right" Width="260">
      <StackPanel Margin="12" Spacing="8">
        <TextBlock Text="Performance Monitor" FontWeight="SemiBold" FontSize="16"
                   Foreground="{DynamicResource SystemControlHighlightAccentBrush}" />

        <TextBlock FontSize="12" Opacity="0.7" TextWrapping="Wrap"
                   Text="Track tab count, live page instances, and managed heap size. Add and remove tabs to observe GC reclaiming memory." />

        <Separator />

        <Button Content="Add 5 Tabs" Click="OnAdd5" HorizontalAlignment="Stretch" />
        <Button Content="Add 20 Tabs" Click="OnAdd20" HorizontalAlignment="Stretch" />
        <Button Content="Remove Last 5" Click="OnRemove5" HorizontalAlignment="Stretch" />
        <Button Content="Clear All" Click="OnClearAll" HorizontalAlignment="Stretch" />

        <Separator />

        <Button Content="Force GC" Click="OnForceGC" HorizontalAlignment="Stretch" />
        <Button Content="Refresh Stats" Click="OnRefresh" HorizontalAlignment="Stretch" />

        <Separator />

        <TextBlock Text="Statistics" FontWeight="SemiBold" FontSize="14" />
        <TextBlock x:Name="TabCountText" Text="Tab count: 0" FontSize="12" />
        <TextBlock x:Name="LiveCountText" Text="Live instances: 0" FontSize="12" />
        <TextBlock x:Name="HeapText" Text="Heap: 0 KB" FontSize="12" />
        <TextBlock x:Name="AllocText" Text="Total allocated: 0 KB" FontSize="12" />
        <TextBlock x:Name="LastOpTimeText" Text="Last Op: —" FontSize="12" />
      </StackPanel>
    </ScrollViewer>

    <Border DockPanel.Dock="Right" Width="1" Background="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />

    <Border Margin="12"
            BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
            BorderThickness="1"
            CornerRadius="6"
            ClipToBounds="True">
      <TabbedPage x:Name="DemoTabs" TabPlacement="Top" />
    </Border>
  </DockPanel>
</UserControl>
