<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="using:ControlCatalog.Pages"
             xmlns:converters="using:ControlCatalog.Converter"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="ControlCatalog.Pages.DirectionalPadControl"
             x:Name="DirectionalPadControlRoot"
             >
  <Grid ColumnDefinitions="*,*,*" RowDefinitions="*,*,*">
    <Grid.Styles>
      <Style Selector="TextBlock">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="Black"/>
      </Style>
    </Grid.Styles>
    <Border Background="White" CornerRadius="8" Grid.Column="1" IsVisible="{Binding Path=#DirectionalPadControlRoot.PadUp}">
      <TextBlock Text="↑" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2" FontSize="18"/>
    </Border>
    <Border Background="White" CornerRadius="8" Grid.Column="0" Grid.Row="1" IsVisible="{Binding Path=#DirectionalPadControlRoot.PadLeft}">
      <TextBlock Text="←" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2" FontSize="18"/>
    </Border>
    <Border Background="White" CornerRadius="8" Grid.Column="2" Grid.Row="1" IsVisible="{Binding Path=#DirectionalPadControlRoot.PadRight}">
      <TextBlock Text="→" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2" FontSize="18"/>
    </Border>
    <Border Background="White" CornerRadius="8" Grid.Column="1" Grid.Row="2" IsVisible="{Binding Path=#DirectionalPadControlRoot.PadDown}">
      <TextBlock Text="↓" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2" FontSize="18"/>
    </Border>
  </Grid>
  
</UserControl>
