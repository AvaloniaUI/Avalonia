<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="ControlCatalog.Pages.NavigationPageGesturePage">
  <DockPanel>
    <ScrollViewer DockPanel.Dock="Right" Width="260">
      <StackPanel Margin="12" Spacing="8">
        <TextBlock Text="Back Gesture" FontWeight="SemiBold" FontSize="14" />

        <CheckBox x:Name="GestureCheck"
                  Content="IsGestureEnabled"
                  IsChecked="True"
                  IsCheckedChanged="OnGestureEnabledChanged" />

        <Separator />

        <TextBlock Text="How it works:"
                   FontWeight="SemiBold"
                   FontSize="12" />
        <TextBlock Text="• Drag from the left edge of the preview toward the right&#10;• Release past the midpoint to complete navigation&#10;• Release before midpoint to cancel"
                   FontSize="11"
                   Opacity="0.7"
                   TextWrapping="Wrap" />

        <Separator />

        <Button Content="Push Pages"
                HorizontalAlignment="Stretch"
                Click="OnPushPages" />
        <Button Content="Pop"
                HorizontalAlignment="Stretch"
                Click="OnPop" />

        <TextBlock x:Name="StatusText"
                   Text="Depth: 1"
                   Opacity="0.7" />
      </StackPanel>
    </ScrollViewer>

    <Border DockPanel.Dock="Right" Width="1" Background="{DynamicResource SystemControlForegroundBaseMediumLowBrush}" />

    <Border Margin="12"
            BorderBrush="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"
            BorderThickness="1"
            CornerRadius="6"
            ClipToBounds="True">
      <NavigationPage x:Name="DemoNav"
                      IsGestureEnabled="True" />
    </Border>
  </DockPanel>
</UserControl>
