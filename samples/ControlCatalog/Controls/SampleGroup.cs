using Avalonia.Controls.Primitives;

namespace ControlCatalog.Controls
{
    /// <summary>
    /// A titled run of <see cref="SampleSection"/>s on a <see cref="SamplePage"/>. Its
    /// <see cref="HeaderedItemsControl.Header"/> names the group ("Overview", "Appearance", ...),
    /// so every page presents the same groups in the same order.
    /// </summary>
    public class SampleGroup : HeaderedItemsControl
    {
    }
}
