namespace Avalonia.Wayland
{
    internal class TextInputInputContext : IInputContext
    {
        /// <inheritdoc />
        public bool HandleEvent(WlWindow window, ref KeyboardInputState state)
        {
            throw new System.NotImplementedException();
        }
    }
}
