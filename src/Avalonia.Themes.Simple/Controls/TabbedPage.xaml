<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Design.PreviewWith>
    <Border Width="400" Height="400">
      <TabbedPage>
        <ContentPage Header="Home">
          <TextBlock Text="Home page" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </ContentPage>
        <ContentPage Header="Favorites">
          <TextBlock Text="Favorites page" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </ContentPage>
      </TabbedPage>
    </Border>
  </Design.PreviewWith>

  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundUnselected" Color="Transparent" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundSelected" Color="Transparent" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundUnselectedPointerOver" Color="{DynamicResource ThemeControlMidBrush}" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundSelectedPointerOver" Color="Transparent" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundUnselectedPressed" Color="{DynamicResource ThemeControlHighlightMidBrush}" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundSelectedPressed" Color="Transparent" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderBackgroundDisabled" Color="Transparent" />

  <!-- Foregrounds (default values; used via TabbedPage.SelectedTabBrush / UnselectedTabBrush) -->
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderForegroundUnselected" Color="{DynamicResource ThemeForegroundMidColor}" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderForegroundSelected" Color="{DynamicResource ThemeAccentColor}" />
  <SolidColorBrush x:Key="TabbedPageTabItemHeaderForegroundDisabled" Color="{DynamicResource ThemeDisabledColor}" />

  <x:Double x:Key="TabbedPageTabItemHeaderMinHeight">48</x:Double>
  <x:Double x:Key="TabbedPageTabItemHeaderVerticalPipeHeight">18</x:Double>
  <x:Double x:Key="TabbedPageTabItemHeaderPipeThickness">2</x:Double>

  <CornerRadius x:Key="TabbedPageTabItemHeaderIndicatorCornerRadius">0</CornerRadius>
  <Thickness x:Key="TabbedPageTabItemHeaderIndicatorMargin">0</Thickness>

  <!-- Tab item sizing & shape -->
  <CornerRadius x:Key="TabbedPageTabItemHeaderCornerRadius">0</CornerRadius>
  <Thickness x:Key="TabbedPageTabItemHeaderPadding">12,8</Thickness>
  <Thickness x:Key="TabbedPageTabItemHeaderMargin">0</Thickness>

  <!-- Icon + text header layout -->
  <x:Double x:Key="TabbedPageTabItemHeaderIconSize">20</x:Double>
  <x:Double x:Key="TabbedPageTabItemHeaderFontSize">12</x:Double>
  <x:Double x:Key="TabbedPageTabItemHeaderSpacing">4</x:Double>

  <!-- Tab strip (bar) styling -->
  <SolidColorBrush x:Key="TabbedPageTabStripBackground" Color="Transparent" />
  <CornerRadius x:Key="TabbedPageTabStripCornerRadius">0</CornerRadius>
  <Thickness x:Key="TabbedPageTabStripBorderThickness">0</Thickness>
  <SolidColorBrush x:Key="TabbedPageTabStripBorderBrush" Color="Transparent" />
  <Thickness x:Key="TabbedPageTabStripMargin">0</Thickness>
  <Thickness x:Key="TabbedPageTabStripPadding">0</Thickness>

  <!-- TabbedPage TabControl theme -->
  <ControlTheme x:Key="TabbedPageTabControlTheme" TargetType="TabControl">
    <Setter Property="Background" Value="{DynamicResource TabbedPageTabStripBackground}" />
    <Setter Property="CornerRadius" Value="{DynamicResource TabbedPageTabStripCornerRadius}" />
    <Setter Property="BorderBrush" Value="{DynamicResource TabbedPageTabStripBorderBrush}" />
    <Setter Property="BorderThickness" Value="{DynamicResource TabbedPageTabStripBorderThickness}" />
    <Setter Property="Margin" Value="{DynamicResource TabbedPageTabStripMargin}" />
    <Setter Property="Padding" Value="{DynamicResource TabbedPageTabStripPadding}" />
    <Setter Property="Template">
      <ControlTemplate>
        <DockPanel>
          <Border Name="PART_TabStrip"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  Margin="{TemplateBinding Margin}"
                  Padding="{TemplateBinding Padding}"
                  DockPanel.Dock="{TemplateBinding TabStripPlacement}">
            <ItemsPresenter Name="PART_ItemsPresenter">
              <ItemsPresenter.ItemsPanel>
                <ItemsPanelTemplate>
                  <UniformGrid Rows="1" />
                </ItemsPanelTemplate>
              </ItemsPresenter.ItemsPanel>
            </ItemsPresenter>
          </Border>
          <Panel ClipToBounds="True">
            <ContentPresenter Name="PART_SelectedContentHost2"
                              HorizontalContentAlignment="Stretch"
                              VerticalContentAlignment="Stretch"
                              IsVisible="False" />
            <ContentPresenter Name="PART_SelectedContentHost"
                              HorizontalContentAlignment="Stretch"
                              VerticalContentAlignment="Stretch" />
          </Panel>
        </DockPanel>
      </ControlTemplate>
    </Setter>

    <Style Selector="^[TabStripPlacement=Left] /template/ ItemsPresenter#PART_ItemsPresenter > UniformGrid,
           ^[TabStripPlacement=Right] /template/ ItemsPresenter#PART_ItemsPresenter > UniformGrid">
      <Setter Property="Rows" Value="0" />
      <Setter Property="Columns" Value="1" />
    </Style>
  </ControlTheme>

  <!-- TabbedPage TabItem theme -->
  <ControlTheme x:Key="TabbedPageTabItemTheme" TargetType="TabItem">
    <Setter Property="FontSize" Value="13" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundUnselected}" />
    <Setter Property="Foreground" Value="{DynamicResource TabbedPageTabItemHeaderForegroundUnselected}" />
    <Setter Property="Padding" Value="{DynamicResource TabbedPageTabItemHeaderPadding}" />
    <Setter Property="Margin" Value="{DynamicResource TabbedPageTabItemHeaderMargin}" />
    <Setter Property="MinHeight" Value="{DynamicResource TabbedPageTabItemHeaderMinHeight}" />
    <Setter Property="CornerRadius" Value="{DynamicResource TabbedPageTabItemHeaderCornerRadius}" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="IndicatorTemplate">
      <DataTemplate>
        <Border Background="{Binding $parent[TabItem].Foreground}"
                CornerRadius="{DynamicResource TabbedPageTabItemHeaderIndicatorCornerRadius}"
                Margin="{DynamicResource TabbedPageTabItemHeaderIndicatorMargin}"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch" />
      </DataTemplate>
    </Setter>
    <Setter Property="Template">
      <ControlTemplate>
        <Panel>
          <Border Name="PART_LayoutRoot"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  Padding="{TemplateBinding Padding}">
            <StackPanel Name="PART_HeaderPanel"
                        Spacing="{DynamicResource TabbedPageTabItemHeaderSpacing}"
                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
              <ContentPresenter Name="PART_IconPresenter"
                                Content="{TemplateBinding Icon}"
                                HorizontalAlignment="Center"
                                IsVisible="{TemplateBinding Icon, Converter={x:Static ObjectConverters.IsNotNull}}"
                                Width="{DynamicResource TabbedPageTabItemHeaderIconSize}"
                                Height="{DynamicResource TabbedPageTabItemHeaderIconSize}" />
              <ContentPresenter Name="PART_ContentPresenter"
                                HorizontalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{DynamicResource TabbedPageTabItemHeaderFontSize}"
                                FontWeight="{TemplateBinding FontWeight}" />
            </StackPanel>
          </Border>
          <ContentPresenter Name="PART_SelectedPipe"
                            VerticalAlignment="Bottom"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Content="{TemplateBinding DataContext}"
                            ContentTemplate="{TemplateBinding IndicatorTemplate}"
                            IsVisible="False" />
        </Panel>
      </ControlTemplate>
    </Setter>

    <!-- Selected state -->
    <Style Selector="^:selected">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundSelected}" />
      <Setter Property="Foreground" Value="{DynamicResource TabbedPageTabItemHeaderForegroundSelected}" />
    </Style>
    <Style Selector="^:selected /template/ ContentPresenter#PART_SelectedPipe">
      <Setter Property="IsVisible" Value="True" />
    </Style>

    <!-- Placement-specific pipe positioning -->
    <Style Selector="^[TabStripPlacement=Top] /template/ ContentPresenter#PART_SelectedPipe">
      <Setter Property="VerticalAlignment" Value="Bottom" />
      <Setter Property="Height" Value="{DynamicResource TabbedPageTabItemHeaderPipeThickness}" />
    </Style>
    <Style Selector="^[TabStripPlacement=Bottom] /template/ ContentPresenter#PART_SelectedPipe">
      <Setter Property="VerticalAlignment" Value="Top" />
      <Setter Property="Height" Value="{DynamicResource TabbedPageTabItemHeaderPipeThickness}" />
    </Style>
    <Style Selector="^[TabStripPlacement=Right]">
      <Setter Property="IndicatorTemplate">
        <DataTemplate>
          <Border Background="{Binding $parent[TabItem].Foreground}"
                  CornerRadius="{DynamicResource TabbedPageTabItemHeaderIndicatorCornerRadius}"
                  Margin="{DynamicResource TabbedPageTabItemHeaderIndicatorMargin}"
                  Width="{DynamicResource TabbedPageTabItemHeaderPipeThickness}"
                  Height="{DynamicResource TabbedPageTabItemHeaderVerticalPipeHeight}" />
        </DataTemplate>
      </Setter>
    </Style>
    <Style Selector="^[TabStripPlacement=Right] /template/ ContentPresenter#PART_SelectedPipe">
      <Setter Property="HorizontalAlignment" Value="Right" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style Selector="^[TabStripPlacement=Left]">
      <Setter Property="IndicatorTemplate">
        <DataTemplate>
          <Border Background="{Binding $parent[TabItem].Foreground}"
                  CornerRadius="{DynamicResource TabbedPageTabItemHeaderIndicatorCornerRadius}"
                  Margin="{DynamicResource TabbedPageTabItemHeaderIndicatorMargin}"
                  Width="{DynamicResource TabbedPageTabItemHeaderPipeThickness}"
                  Height="{DynamicResource TabbedPageTabItemHeaderVerticalPipeHeight}" />
        </DataTemplate>
      </Setter>
    </Style>
    <Style Selector="^[TabStripPlacement=Left] /template/ ContentPresenter#PART_SelectedPipe">
      <Setter Property="HorizontalAlignment" Value="Left" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- Pointer-over -->
    <Style Selector="^:pointerover /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundUnselectedPointerOver}" />
    </Style>

    <!-- Selected + pointer-over -->
    <Style Selector="^:selected:pointerover /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundSelectedPointerOver}" />
    </Style>

    <!-- Pressed -->
    <Style Selector="^:pressed /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundUnselectedPressed}" />
    </Style>

    <!-- Selected + pressed -->
    <Style Selector="^:selected:pressed /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundSelectedPressed}" />
    </Style>

    <!-- Disabled -->
    <Style Selector="^:disabled /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource TabbedPageTabItemHeaderBackgroundDisabled}" />
      <Setter Property="TextElement.Foreground" Value="{DynamicResource TabbedPageTabItemHeaderForegroundDisabled}" />
    </Style>

    <!-- Focus visible -->
    <Style Selector="^:focus-visible /template/ Border#PART_LayoutRoot">
      <Setter Property="BorderBrush" Value="{DynamicResource ThemeBorderHighBrush}" />
      <Setter Property="BorderThickness" Value="2" />
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="{x:Type TabbedPage}" TargetType="TabbedPage">
    <Setter Property="Background" Value="{DynamicResource ThemeBackgroundBrush}" />
    <Setter Property="Template">
      <ControlTemplate>
        <Panel Background="{TemplateBinding Background}">
          <TabControl Name="PART_TabControl"
                      Theme="{StaticResource TabbedPageTabControlTheme}"
                      ItemContainerTheme="{StaticResource TabbedPageTabItemTheme}"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ItemsSource="{TemplateBinding Pages}" />
        </Panel>
      </ControlTemplate>
    </Setter>
  </ControlTheme>
</ResourceDictionary>
