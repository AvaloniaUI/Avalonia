<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="0">
      <ListBox>
        <ListBoxItem IsEnabled="False">Disabled</ListBoxItem>
        <ListBoxItem Background="#66000000"
                     Padding="20">
          Test
        </ListBoxItem>
        <ListBoxItem IsSelected="True">Selected</ListBoxItem>
        <ListBoxItem>Test</ListBoxItem>
      </ListBox>
    </Border>
  </Design.PreviewWith>

  <!-- WinUI reference: ListViewItem_themeresources.xaml (ListViewItemExpanded, the live default).
  Rounded WinUI 3 list items: backplate inset from the list edges, CornerRadius 4, MinHeight 40,
  transparent rest state, subtle-fill hover/press/selection and an accent selection pill
  (3x16, r1.5, vertically centered on the left edge) that only shows when selected.
  The 4,2 backplate inset and the pill metrics are baked into WinUI's ListViewItemPresenter
  rather than exposed as theme resources, so they are approximated here from the WinUI visuals. -->
  <Thickness x:Key="ListBoxItemFluentPadding">12,0,12,0</Thickness>
  <Thickness x:Key="ListBoxItemPadding">12,9,12,12</Thickness>
  <x:Double x:Key="ListBoxItemMinHeight">40</x:Double>
  <x:Double x:Key="ListBoxItemMinWidth">88</x:Double>
  <ControlTheme x:Key="{x:Type ListBoxItem}" TargetType="ListBoxItem" BasedOn="{StaticResource FluentListBoxItemTheme}"/>
  <ControlTheme x:Key="FluentListBoxItemTheme" TargetType="ListBoxItem">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
    <Setter Property="Padding" Value="{DynamicResource ListBoxItemFluentPadding}" />
    <Setter Property="MinHeight" Value="{DynamicResource ListBoxItemMinHeight}" />
    <Setter Property="MinWidth" Value="{DynamicResource ListBoxItemMinWidth}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="Template">
      <ControlTemplate>
        <!-- WinUI's ListViewItemPresenter insets the rounded backplate from the item bounds. -->
        <Panel Margin="4,2">
          <ContentPresenter Name="PART_ContentPresenter"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}"
                                ContentTemplate="{TemplateBinding ContentTemplate}"
                                Content="{TemplateBinding Content}"
                            Padding="{TemplateBinding Padding}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" />
          <!-- Selection pill: WinUI animates its height 0 -> 16 over 167ms when the item is selected;
               expressed here as a scaleY render transform so the animation doesn't run layout every
               frame (technique from FluentAvalonia). ListViewItemSelectionIndicatorBrush is not aliased
               in this theme yet, so the Fluent 2 token AccentFillColorDefaultBrush is referenced directly. -->
          <Rectangle Name="SelectionIndicator"
                     IsVisible="False"
                     Width="3"
                     Height="16"
                     RadiusX="1.5"
                     RadiusY="1.5"
                     HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                     IsHitTestVisible="False"
                     UseLayoutRounding="False"
                     RenderTransform="scaleY(0)"
                     Fill="{DynamicResource ListViewItemSelectionIndicatorBrush}">
            <Rectangle.Transitions>
              <Transitions>
                <TransformOperationsTransition Property="RenderTransform" Duration="{StaticResource ControlFastAnimationDuration}" Easing="0,0 0,1" />
              </Transitions>
            </Rectangle.Transitions>
          </Rectangle>
        </Panel>
      </ControlTemplate>
    </Setter>

    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForegroundDisabled}" />
    </Style>

    <!--  PointerOver State  -->
    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPointerOver}" />
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
    </Style>

    <!--  Pressed State  -->
    <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPressed}" />
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
    </Style>

    <!--  Selected State  -->
    <Style Selector="^:selected">

      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPointerOver}" />
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>

      <Style Selector="^ /template/ Rectangle#SelectionIndicator">
        <Setter Property="IsVisible" Value="True" />
        <Setter Property="RenderTransform" Value="scaleY(1)" />
      </Style>

      <Style Selector="^:disabled /template/ Rectangle#SelectionIndicator">
        <Setter Property="Fill" Value="{DynamicResource ListViewItemSelectionIndicatorDisabledBrush}" />
      </Style>

      <!--  Selected Unfocused State  -->
      <Style Selector="^:not(:focus) /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPointerOver}" />
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>

      <!--  Selected PointerOver State  -->
      <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPressed}" />
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>

      <!--  Selected Pressed State  -->
      <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ListBoxItemBackgroundPointerOver}" />
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="DefaultListBoxItemStyle" TargetType="ListBoxItem">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Padding" Value="{DynamicResource ListBoxItemPadding}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border x:Name="LayoutRoot" Background="{TemplateBinding Background}"
                BorderThickness="{TemplateBinding BorderThickness}">
          <Grid>
            <Rectangle x:Name="PressedBackground" Fill="Transparent" TemplatedControl.IsTemplateFocusTarget="True" />
            <ContentPresenter x:Name="PART_ContentPresenter"
                              Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              TextWrapping="NoWrap"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="{TemplateBinding Padding}" />
          </Grid>
        </Border>
      </ControlTemplate>
    </Setter>
    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForegroundDisabled}" />
    </Style>

    <!--  PointerOver State  -->
    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
    </Style>
    <Style Selector="^:pointerover /template/ Rectangle#PressedBackground">
      <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundPointerOver}" />
    </Style>

    <!--  Pressed State  -->
    <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
    </Style>
    <Style Selector="^:pointerover /template/ Rectangle#PressedBackground">
      <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundPressed}" />
    </Style>

    <!--  Selected State  -->
    <Style Selector="^:selected">

      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>
      <Style Selector="^ /template/ Rectangle#PressedBackground">
        <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundSelected}" />
      </Style>

      <!--  Selected Unfocused State  -->
      <Style Selector="^:not(:focus) /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>
      <Style Selector="^:not(:focus) /template/ Rectangle#PressedBackground">
        <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundSelected}" />
      </Style>

      <!--  Selected PointerOver State  -->
      <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>
      <Style Selector="^:pointerover /template/ Rectangle#PressedBackground">
        <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundSelectedPointerOver}" />
      </Style>

      <!--  Selected Pressed State  -->
      <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource ListBoxItemForeground}" />
      </Style>
      <Style Selector="^:pressed /template/ Rectangle#PressedBackground">
        <Setter Property="Fill" Value="{DynamicResource ListBoxItemBackgroundSelectedPressed}" />
      </Style>
    </Style>
  </ControlTheme>
</ResourceDictionary>
