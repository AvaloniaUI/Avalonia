<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Design.PreviewWith>
    <Border Width="400" Height="300">
      <CarouselPage>
        <ContentPage Header="Page 1">
          <TextBlock Text="First page" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </ContentPage>
        <ContentPage Header="Page 2">
          <TextBlock Text="Second page" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </ContentPage>
      </CarouselPage>
    </Border>
  </Design.PreviewWith>  

  <ResourceDictionary.ThemeDictionaries>
    <ResourceDictionary x:Key="Default">
      <StaticResource x:Key="CarouselPageBackground" ResourceKey="SolidBackgroundFillColorBaseBrush" />
    </ResourceDictionary>
    <ResourceDictionary x:Key="Dark">
      <StaticResource x:Key="CarouselPageBackground" ResourceKey="SolidBackgroundFillColorBaseBrush" />
    </ResourceDictionary>
  </ResourceDictionary.ThemeDictionaries>

  <ControlTheme x:Key="{x:Type CarouselPage}" TargetType="CarouselPage" BasedOn="{StaticResource DefaultCarouselPageTheme}"/>

  <ControlTheme x:Key="DefaultCarouselPageTheme" TargetType="CarouselPage">
    <Setter Property="Background" Value="{DynamicResource CarouselPageBackground}" />
    <Setter Property="Template">
      <ControlTemplate>
        <Carousel Name="PART_Carousel"
                  Background="{TemplateBinding Background}"
                  ItemTemplate="{TemplateBinding PageTemplate}"
                  PageTransition="{TemplateBinding PageTransition}"
                  ItemsPanel="{TemplateBinding ItemsPanel}"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" />
      </ControlTemplate>
    </Setter>
  </ControlTheme>
</ResourceDictionary>
