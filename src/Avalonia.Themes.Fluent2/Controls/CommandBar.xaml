<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:ClassModifier="internal">
  <Design.PreviewWith>
    <Border Padding="20" Width="600">
      <StackPanel Spacing="12">
        <CommandBar>
          <CommandBarButton Label="New">
            <CommandBarButton.Icon>
              <PathIcon Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z" />
            </CommandBarButton.Icon>
          </CommandBarButton>
          <CommandBarButton Label="Save">
            <CommandBarButton.Icon>
              <PathIcon Data="M15,9H5V5H15M12,19A3,3 0 0,1 9,16A3,3 0 0,1 12,13A3,3 0 0,1 15,16A3,3 0 0,1 12,19M17,3H5C3.89,3 3,3.9 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V7L17,3Z" />
            </CommandBarButton.Icon>
          </CommandBarButton>
          <CommandBarSeparator />
          <CommandBarToggleButton Label="Bold">
            <CommandBarToggleButton.Icon>
              <PathIcon Data="M15.6,10.79C17.04,10.07 18,8.64 18,7C18,4.79 16.21,3 14,3H7V21H14.73C16.78,21 18.5,19.37 18.5,17.32C18.5,15.82 17.72,14.53 16.5,13.77C16.2,13.59 15.9,13.44 15.6,13.32V10.79M10,6.5H13C13.83,6.5 14.5,7.17 14.5,8C14.5,8.83 13.83,9.5 13,9.5H10V6.5M13.5,17.5H10V14H13.5C14.33,14 15,14.67 15,15.5C15,16.33 14.33,17.5 13.5,17.5Z" />
            </CommandBarToggleButton.Icon>
          </CommandBarToggleButton>
        </CommandBar>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <x:Double x:Key="CommandBarExpandButtonThemeWidth">48</x:Double>
  <Thickness x:Key="CommandBarEllipsisButtonInnerBorderMargin">2,6,6,6</Thickness>
  <Thickness x:Key="CommandBarInnerBorderMargin">2,6,2,6</Thickness>
  <Thickness x:Key="CommandBarButtonInnerBorderCompactMargin">2,6,2,22</Thickness>
  <Thickness x:Key="CommandBarButtonContentViewboxMargin">12,16,0,10</Thickness>
  <Thickness x:Key="CommandBarButtonTextLabelOnRightMargin">8,16,12,10</Thickness>
  <x:Double x:Key="CommandBarThemeMinHeight">64</x:Double>
  <x:Double x:Key="CommandBarThemeCompactHeight">48</x:Double>
  <x:Double x:Key="CommandButtonContentHeight">16</x:Double>
  <Thickness x:Key="CommandButtonContentViewboxCollapsedMargin">0,16,0,2</Thickness>
  <x:Double x:Key="CommandBarButtonLabelFontSize">12</x:Double>
  <Thickness x:Key="CommandBarButtonTextLabelMargin">2,0,2,8</Thickness>
  <Thickness x:Key="CommandBarButtonOverflowTextLabelPadding">0,5,0,8</Thickness>
  <Thickness x:Key="CommandBarButtonInnerBorderOverflowMargin">4,0,4,0</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterMargin">0,4,0,4</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterBorderPadding">0</Thickness>
  <Thickness x:Key="CommandBarSeparatorMargin">2,8,2,8</Thickness>
  <Thickness x:Key="CommandBarOverflowSeparatorMargin">0,4,0,4</Thickness>
  <x:Double x:Key="CommandBarSeparatorWidth">1</x:Double>
  <x:Double x:Key="CommandBarOverflowSeparatorHeight">1</x:Double>
  <x:Double x:Key="CommandBarSeparatorCornerRadius">0.5</x:Double>
  <Thickness x:Key="CommandBarBorderThicknessOpen">1</Thickness>
  <x:Double x:Key="CommandBarOverflowMinWidth">160</x:Double>
  <x:Double x:Key="CommandBarOverflowTouchMinWidth">240</x:Double>
  <x:Double x:Key="CommandBarOverflowMaxWidth">480</x:Double>
  <x:Double x:Key="CommandBarOverflowMaxHeight">198</x:Double>
  <Thickness x:Key="CommandBarOverflowPresenterBorderThickness">1</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterBorderDownThickness">0,0,0,1</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterBorderUpThickness">0,1,0,0</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterBorderDownPadding">0</Thickness>
  <Thickness x:Key="CommandBarOverflowPresenterBorderUpPadding">0</Thickness>
  <x:Double x:Key="CommandBarMoreButtonColumnMinWidth">6</x:Double>
  <Thickness x:Key="CommandBarOverflowButtonMargin">14,23,14,0</Thickness>

  <!-- WinUI reference: CommandBar_themeresources.xaml, AppBarButton_themeresources.xaml (winui3/release/1.6.4),
  AppBarToggleButton_themeresources.xaml, AppBarSeparator_themeresources.xaml (via Uno.UI.FluentTheme.v2).
  AppBar idiom: transparent bar (CommandBarBackground = ControlFillColorTransparent; the open state uses
  AcrylicInAppFillColorDefault with a 1px CardStrokeColorDefaultSolid border), subtle-fill command pills
  with ControlCornerRadius and an 83ms fill cross-fade, 16px icons (AppBarButtonContentHeight) with 12px
  labels, checked toggles on AccentFillColorDefault with TextOnAccent text, and 1px
  DividerStrokeColorDefault separators (AppBarSeparatorMargin 2,8). WinUI's default closed CommandBar has
  no bottom divider, so none is drawn here. The v1 SystemControl* brush references were re-pointed to the
  Fluent 2 tokens in this file; no shared-file keys are defined for these controls. -->

  <!-- CommandBar ellipsis (more) button: same subtle-fill treatment as AppBarButton. -->
  <ControlTheme x:Key="Fluent2CommandBarEllipsisButtonTheme" TargetType="Button">
    <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrush}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="Width" Value="{DynamicResource CommandBarExpandButtonThemeWidth}" />
    <Setter Property="Template">
      <ControlTemplate>
        <ContentPresenter x:Name="PART_ContentPresenter"
                          Background="{TemplateBinding Background}"
                          BackgroundSizing="{TemplateBinding BackgroundSizing}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          CornerRadius="{TemplateBinding CornerRadius}"
                          Content="{TemplateBinding Content}"
                          ContentTemplate="{TemplateBinding ContentTemplate}"
                          Padding="{TemplateBinding Padding}"
                          Margin="{StaticResource CommandBarEllipsisButtonInnerBorderMargin}"
                          TemplatedControl.IsTemplateFocusTarget="True"
                          HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                          VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
          <ContentPresenter.Transitions>
            <Transitions>
              <BrushTransition Property="Background" Duration="{DynamicResource ControlFasterAnimationDuration}" />
            </Transitions>
          </ContentPresenter.Transitions>
        </ContentPresenter>
      </ControlTemplate>
    </Setter>

    <Style Selector="^:pointerover /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundPointerOver}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushPointerOver}" />
    </Style>
    <Style Selector="^:pointerover">
      <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPointerOver}" />
    </Style>
    <Style Selector="^:pressed /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundPressed}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushPressed}" />
    </Style>
    <Style Selector="^:pressed">
      <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPressed}" />
    </Style>
    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundDisabled}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushDisabled}" />
    </Style>
    <Style Selector="^:disabled">
      <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundDisabled}" />
    </Style>
  </ControlTheme>
  <ControlTheme x:Key="CommandButtonOverflowStyle" TargetType="CommandBarButton" BasedOn="{StaticResource DefaultFluentCommandBarButtonStyle}">
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="Width" Value="NaN" />
  </ControlTheme>
  <ControlTheme x:Key="{x:Type CommandBarButton}" TargetType="CommandBarButton" BasedOn="{StaticResource DefaultFluentCommandBarButtonStyle}"/>

  <!-- CommandBarButton ControlTheme (WinUI DefaultAppBarButtonStyle) -->
  <ControlTheme x:Key="DefaultFluentCommandBarButtonStyle" TargetType="CommandBarButton">
    <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrush}" />
    <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="Padding" Value="8,4" />
    <Setter Property="Width" Value="68" />
    <Setter Property="MinHeight" Value="36" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <ControlTemplate>
        <Grid Name="RootGrid"
              Background="Transparent"
              MinWidth="{TemplateBinding MinWidth}"
              MaxWidth="{TemplateBinding MaxWidth}">
          <Border Name="PART_Border"
                  Margin="{DynamicResource CommandBarInnerBorderMargin}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  BackgroundSizing="{TemplateBinding BackgroundSizing}">
            <Border.Transitions>
              <Transitions>
                <BrushTransition Property="Background" Duration="{DynamicResource ControlFasterAnimationDuration}" />
              </Transitions>
            </Border.Transitions>
          </Border>
          <Grid Name="LayoutGrid"
                MinHeight="{DynamicResource CommandBarThemeMinHeight}">
            <StackPanel Name="PART_LayoutRoot"
                        Orientation="Vertical"
                        VerticalAlignment="Center"
                        Spacing="6">
              <Viewbox Name="ContentViewbox"
                       Height="{DynamicResource CommandButtonContentHeight}"
                       HorizontalAlignment="Stretch">
                <ContentPresenter Name="PART_IconPresenter"
                                  Content="{TemplateBinding Icon}"
                                  Foreground="{TemplateBinding Foreground}" />
              </Viewbox>
              <TextBlock Name="PART_Label"
                         Text="{TemplateBinding Label}"
                         Foreground="{TemplateBinding Foreground}"
                         FontSize="{DynamicResource CommandBarButtonLabelFontSize}"
                         FontFamily="{TemplateBinding FontFamily}"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         TextTrimming="CharacterEllipsis" />
            </StackPanel>
          </Grid>
        </Grid>
      </ControlTemplate>
    </Setter>

    <!-- Hover -->
    <Style Selector="^:pointerover">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundPointerOver}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushPointerOver}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPointerOver}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPointerOver}" />
      </Style>
    </Style>

    <!-- Pressed -->
    <Style Selector="^:pressed">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundPressed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushPressed}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPressed}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundPressed}" />
      </Style>
    </Style>

    <!-- Disabled -->
    <Style Selector="^:disabled">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarButtonBackgroundDisabled}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarButtonBorderBrushDisabled}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundDisabled}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarButtonForegroundDisabled}" />
      </Style>
    </Style>

    <!-- Compact: hide label -->
    <Style Selector="^[IsCompact=True]">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Margin" Value="{DynamicResource CommandBarButtonInnerBorderCompactMargin}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="IsVisible" Value="False" />
      </Style>
    </Style>

    <!-- LabelPosition=Right: horizontal layout, auto-width -->
    <Style Selector="^[LabelPosition=Right]">
      <Style Selector="^ /template/ StackPanel#PART_LayoutRoot">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="12"/>
      </Style>
      <Style Selector="^ /template/ Grid#LayoutGrid">
        <Setter Property="MinHeight" Value="{DynamicResource CommandBarThemeCompactHeight}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Left" />
      </Style>
    </Style>

    <!-- IsInOverflow: full-width horizontal layout -->
    <Style Selector="^[IsInOverflow=True]">
      <Style Selector="^ /template/ Grid#LayoutGrid">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="MinHeight" Value="0" />
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Margin" Value="{DynamicResource CommandBarButtonInnerBorderOverflowMargin}" />
      </Style>

      <Style Selector="^ /template/ Viewbox#ContentViewbox">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Margin" Value="12,0,0,0" />
      </Style>
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="CommandToggleButtonOverflowStyle" TargetType="CommandBarToggleButton" BasedOn="{StaticResource DefaultCommandBarToggleButtonStyle}">
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="Width" Value="NaN" />
  </ControlTheme>
  <ControlTheme x:Key="{x:Type CommandBarToggleButton}" TargetType="CommandBarToggleButton" BasedOn="{StaticResource DefaultCommandBarToggleButtonStyle}"/>

  <!-- CommandBarToggleButton ControlTheme (WinUI DefaultAppBarToggleButtonStyle) -->
  <ControlTheme x:Key="DefaultCommandBarToggleButtonStyle" TargetType="CommandBarToggleButton">
    <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrush}" />
    <Setter Property="BackgroundSizing" Value="InnerBorderEdge" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="Padding" Value="8,4" />
    <Setter Property="Width" Value="68" />
    <Setter Property="MinHeight" Value="36" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <ControlTemplate>
        <Grid Name="RootGrid"
              Background="Transparent"
              MinWidth="{TemplateBinding MinWidth}"
              MaxWidth="{TemplateBinding MaxWidth}">
          <Border Name="PART_Border"
                  Margin="{DynamicResource CommandBarInnerBorderMargin}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  BackgroundSizing="{TemplateBinding BackgroundSizing}">
            <Border.Transitions>
              <Transitions>
                <BrushTransition Property="Background" Duration="{DynamicResource ControlFasterAnimationDuration}" />
              </Transitions>
            </Border.Transitions>
          </Border>
          <Grid Name="LayoutGrid"
                MinHeight="{DynamicResource CommandBarThemeMinHeight}">
            <StackPanel Name="PART_LayoutRoot"
                        Orientation="Vertical"
                        VerticalAlignment="Center"
                        Spacing="6">
              <Viewbox Name="ContentViewbox"
                       Height="{DynamicResource CommandButtonContentHeight}"
                       HorizontalAlignment="Stretch">
                <ContentPresenter Name="PART_IconPresenter"
                                  Content="{TemplateBinding Icon}"
                                  Foreground="{TemplateBinding Foreground}" />
              </Viewbox>
              <TextBlock Name="PART_Label"
                         Text="{TemplateBinding Label}"
                         Foreground="{TemplateBinding Foreground}"
                         FontSize="{DynamicResource CommandBarButtonLabelFontSize}"
                         FontFamily="{TemplateBinding FontFamily}"
                         TextAlignment="Center"
                         TextWrapping="Wrap"
                         TextTrimming="CharacterEllipsis" />
            </StackPanel>
          </Grid>
        </Grid>
      </ControlTemplate>
    </Setter>

    <!-- Hover -->
    <Style Selector="^:pointerover">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundPointerOver}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushPointerOver}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundPointerOver}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundPointerOver}" />
      </Style>
    </Style>

    <!-- Pressed -->
    <Style Selector="^:pressed">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundPressed}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushPressed}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundPressed}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundPressed}" />
      </Style>
    </Style>

    <Style Selector="^:disabled">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundDisabled}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushDisabled}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundDisabled}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundDisabled}" />
      </Style>
    </Style>

    <!-- Checked -->
    <Style Selector="^:checked">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundChecked}" />
        <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushChecked}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundChecked}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundChecked}" />
      </Style>

      <!-- Hover -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundCheckedPointerOver}" />
          <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushCheckedPointerOver}" />
        </Style>
        <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedPointerOver}" />
        </Style>
        <Style Selector="^ /template/ TextBlock#PART_Label">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedPointerOver}" />
        </Style>
      </Style>

      <!-- Pressed -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundCheckedPressed}" />
          <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushCheckedPressed}" />
        </Style>
        <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedPressed}" />
        </Style>
        <Style Selector="^ /template/ TextBlock#PART_Label">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedPressed}" />
        </Style>
      </Style>

      <Style Selector="^:disabled">
        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundCheckedDisabled}" />
          <Setter Property="BorderBrush" Value="{DynamicResource CommandBarToggleButtonBorderBrushCheckedDisabled}" />
        </Style>
        <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedDisabled}" />
        </Style>
        <Style Selector="^ /template/ TextBlock#PART_Label">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonForegroundCheckedDisabled}" />
        </Style>
      </Style>
    </Style>

    <!-- Compact: hide label -->
    <Style Selector="^[IsCompact=True]">
      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Margin" Value="{DynamicResource CommandBarToggleButtonInnerBorderCompactMargin}" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="IsVisible" Value="False" />
      </Style>
    </Style>

    <!-- LabelPosition=Right: horizontal layout, auto-width -->
    <Style Selector="^[LabelPosition=Right]">
      <Style Selector="^ /template/ StackPanel#PART_LayoutRoot">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="Spacing" Value="12"/>
      </Style>
      <Style Selector="^ /template/ Grid#LayoutGrid">
        <Setter Property="MinHeight" Value="{DynamicResource CommandBarThemeCompactHeight}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_IconPresenter">
        <Setter Property="VerticalAlignment" Value="Center" />
      </Style>
      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Left" />
      </Style>
    </Style>

    <!-- IsInOverflow: full-width horizontal layout -->
    <Style Selector="^[IsInOverflow=True]">
      <Style Selector="^ /template/ Grid#LayoutGrid">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="MinHeight" Value="0" />
      </Style>

      <Style Selector="^ /template/ Border#PART_Border">
        <Setter Property="Margin" Value="{DynamicResource CommandBarToggleButtonInnerBorderOverflowMargin}" />
      </Style>

      <Style Selector="^ /template/ Viewbox#ContentViewbox">
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Width" Value="16" />
        <Setter Property="Height" Value="16" />
        <Setter Property="Margin" Value="12,0,0,0" />
      </Style>

      <Style Selector="^ /template/ TextBlock#PART_Label">
        <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonOverflowLabelForegroundPointerOver}" />
      </Style>


      <!-- Hover -->
      <Style Selector="^:pointerover">
        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundHighLightOverlayPointerOver}" />
        </Style>
        <Style Selector="^ /template/ TextBlock#PART_Label">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonOverflowLabelForegroundPointerOver}" />
        </Style>
      </Style>

      <!-- Pressed -->
      <Style Selector="^:pressed">
        <Style Selector="^ /template/ Border#PART_Border">
          <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundHighLightOverlayPressed}" />
        </Style>
        <Style Selector="^ /template/ TextBlock#PART_Label">
          <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonOverflowLabelForegroundPressed}" />
        </Style>
      </Style>

      <!-- Checked -->
      <Style Selector="^:checked">
        <!-- Hover -->
        <Style Selector="^:pointerover">
          <Style Selector="^ /template/ Border#PART_Border">
            <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundHighLightOverlayCheckedPointerOver}" />
          </Style>
          <Style Selector="^ /template/ TextBlock#PART_Label">
            <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonOverflowLabelForegroundCheckedPointerOver}" />
          </Style>
        </Style>

        <!-- Pressed -->
        <Style Selector="^:pressed">
          <Style Selector="^ /template/ Border#PART_Border">
            <Setter Property="Background" Value="{DynamicResource CommandBarToggleButtonBackgroundHighLightOverlayCheckedPressed}" />
          </Style>
          <Style Selector="^ /template/ TextBlock#PART_Label">
            <Setter Property="Foreground" Value="{DynamicResource CommandBarToggleButtonOverflowLabelForegroundCheckedPressed}" />
          </Style>
        </Style>
      </Style>
    </Style>
  </ControlTheme>

  <!-- CommandBarSeparator ControlTheme (WinUI DefaultAppBarSeparatorStyle:
  1px DividerStrokeColorDefault, AppBarSeparatorMargin 2,8, corner radius 0.5) -->
  <ControlTheme x:Key="{x:Type CommandBarSeparator}" TargetType="CommandBarSeparator">
    <Setter Property="Foreground" Value="{DynamicResource CommandSeparatorForeground}" />
    <Setter Property="Padding" Value="{DynamicResource CommandBarSeparatorMargin}" />
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="Template">
      <ControlTemplate>
        <Rectangle Name="SeparatorRectangle"
                   RadiusX="{StaticResource CommandBarSeparatorCornerRadius}"
                   RadiusY="{StaticResource CommandBarSeparatorCornerRadius}"
                   Width="{StaticResource CommandBarSeparatorWidth}"
                   VerticalAlignment="Stretch"
                   Fill="{TemplateBinding Foreground}"
                   Margin="{TemplateBinding Padding}" />
      </ControlTemplate>
    </Setter>

    <!-- IsInOverflow -->
    <Style Selector="^[IsInOverflow=True] /template/ Rectangle#SeparatorRectangle">
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="Width" Value="NaN" />
      <Setter Property="Height" Value="{StaticResource CommandBarOverflowSeparatorHeight}" />
      <Setter Property="Margin" Value="{StaticResource CommandBarOverflowSeparatorMargin}" />
    </Style>
  </ControlTheme>

  <!-- CommandBar ControlTheme (WinUI XamlDefaultCommandBar) -->
  <ControlTheme x:Key="{x:Type CommandBar}" TargetType="CommandBar">
    <Setter Property="Background" Value="{DynamicResource CommandBarBackground}" />
    <Setter Property="Foreground" Value="{DynamicResource CommandBarForeground}" />
    <Setter Property="BorderBrush" Value="{DynamicResource ControlFillColorTransparentBrush}" />
    <Setter Property="CornerRadius" Value="{DynamicResource ControlCornerRadius}" />
    <Setter Property="MinHeight" Value="{DynamicResource CommandBarThemeCompactHeight}" />
    <Setter Property="Padding" Value="4,0" />
    <Setter Property="ItemWidthBottom" Value="70" />
    <Setter Property="ItemWidthRight" Value="102" />
    <Setter Property="ItemWidthCollapsed" Value="42" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="Template">
      <ControlTemplate>
        <Border x:Name="LayoutRoot"
                Background="{TemplateBinding Background}"
                BackgroundSizing="InnerBorderEdge"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="{TemplateBinding CornerRadius}"
                Padding="{TemplateBinding Padding}"
                MinHeight="{TemplateBinding MinHeight}">
          <Panel>
            <DockPanel KeyboardNavigation.TabNavigation="Cycle">
              <!-- Overflow (more/ellipsis) button (docked right) -->
              <Button x:Name="PART_OverflowButton"
                      DockPanel.Dock="Right"
                      Foreground="{TemplateBinding Foreground}"
                      Theme="{DynamicResource Fluent2CommandBarEllipsisButtonTheme}"
                      MinHeight="{DynamicResource CommandBarThemeCompactHeight}"
                      VerticalAlignment="Stretch"
                      IsVisible="{TemplateBinding IsOverflowButtonVisible}">
                <PathIcon Width="20" 
                          Height="20"  
                          VerticalAlignment="Center"
                          Data="M6,10A2,2 0 0,0 4,12A2,2 0 0,0 6,14A2,2 0 0,0 8,12A2,2 0 0,0 6,10M12,10A2,2 0 0,0 10,12A2,2 0 0,0 12,14A2,2 0 0,0 14,12A2,2 0 0,0 12,10M18,10A2,2 0 0,0 16,12A2,2 0 0,0 18,14A2,2 0 0,0 20,12A2,2 0 0,0 18,10Z" />
              </Button>

              <!-- Custom content area (docked left) -->
              <ContentPresenter x:Name="PART_ContentPresenter"
                                Content="{TemplateBinding Content}"
                                DockPanel.Dock="Left"
                                VerticalAlignment="Center" />

              <!-- Primary commands (fills remaining space, right-aligned) -->
              <ItemsControl x:Name="PART_PrimaryCommands"
                            ItemsSource="{Binding VisiblePrimaryCommands, RelativeSource={RelativeSource TemplatedParent}}"
                            HorizontalAlignment="Right"
                            MinHeight="{DynamicResource CommandBarThemeCompactHeight}"
                            VerticalAlignment="Stretch"
                            ClipToBounds="True">
                <ItemsControl.ItemsPanel>
                  <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" />
                  </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
              </ItemsControl>
            </DockPanel>

            <!-- Overflow popup (WinUI overflow presenter: AcrylicInAppFillColorDefault surface,
            SurfaceStrokeColorFlyout 1px border, OverlayCornerRadius, min width 160) -->
            <Popup x:Name="PART_OverflowPopup"
                   IsOpen="{TemplateBinding IsOpen, Mode=TwoWay}"
                   PlacementTarget="{Binding #LayoutRoot}"
                   Placement="BottomEdgeAlignedRight"
                   IsLightDismissEnabled="True"
                   WindowManagerAddShadowHint="True">
              <Border Background="{DynamicResource CommandBarOverflowPresenterBackground}"
                      BackgroundSizing="InnerBorderEdge"
                      BorderBrush="{DynamicResource CommandBarOverflowPresenterBorderBrush}"
                      BorderThickness="{DynamicResource CommandBarOverflowPresenterBorderThickness}"
                      CornerRadius="{DynamicResource OverlayCornerRadius}"
                      Padding="{DynamicResource CommandBarOverflowPresenterBorderPadding}"
                      MinWidth="{DynamicResource CommandBarOverflowMinWidth}"
                      MaxWidth="{DynamicResource CommandBarOverflowMaxWidth}">
                <ItemsControl x:Name="PART_OverflowPresenter"
                              Margin="{DynamicResource CommandBarOverflowPresenterMargin}"
                              ItemContainerTheme="{DynamicResource CommandButtonOverflowStyle}"
                              ItemsSource="{Binding OverflowItems, RelativeSource={RelativeSource TemplatedParent}}">
                  <ItemsControl.Resources>
                    <ControlTheme x:Key="{x:Type CommandBarButton}" TargetType="CommandBarButton" BasedOn="{StaticResource CommandButtonOverflowStyle}"/>
                    <ControlTheme x:Key="{x:Type CommandBarToggleButton}" TargetType="CommandBarToggleButton" BasedOn="{StaticResource CommandToggleButtonOverflowStyle}"/>
                  </ItemsControl.Resources>
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <StackPanel Spacing="2" />
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                </ItemsControl>
              </Border>
            </Popup>
          </Panel>
        </Border>
      </ControlTemplate>
    </Setter>

    <!-- Open state: CommandBarBackgroundOpen / CommandBarBorderBrushOpen (thickness stays 1 to avoid
    a layout jump; the rest-state border is transparent). -->
    <Style Selector="^[IsOpen=True] /template/ Border#LayoutRoot">
      <Setter Property="Background" Value="{DynamicResource AcrylicInAppFillColorDefaultBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource CardStrokeColorDefaultSolidBrush}" />
    </Style>
  </ControlTheme>
</ResourceDictionary>
